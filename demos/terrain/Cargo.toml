[package]
name = "terrain_demo"
version = "0.1.0"
edition = "2021"
publish = false
build = "build.rs"
[package.metadata.wasmcompute]
std = true
[lib]
path = "lib.rs"
[dependencies]
minecraft = { path = "../../sdk/rust/minecraft" }
terrain-kernel = { path = "kernel" }
[build-dependencies]
serde_json = "1"
[workspace]
exclude = ["kernel"]
